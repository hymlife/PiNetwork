import { StyleSheet, Text, View } from "react-native";
import React from "react";

const DestekMerkeziScreen = () => {
  return (
    <View>
      <Text>DestekMerkeziScreen</Text>
    </View>
  );
};

export default DestekMerkeziScreen;

const styles = StyleSheet.create({});
